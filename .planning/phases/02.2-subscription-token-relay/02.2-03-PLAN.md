---
phase: 02.2-subscription-token-relay
plan: 03
type: execute
wave: 2
depends_on: [02.2-01]
files_modified:
  - docs-site/content/ja/docs/configuration.md
  - docs-site/content/zh-cn/docs/configuration.md
  - docs-site/content/ko/docs/configuration.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Japanese docs explain two-tier auth model in Japanese"
    - "Chinese docs explain two-tier auth model in Simplified Chinese"
    - "Korean docs explain two-tier auth model in Korean"
    - "YAML code blocks remain in English for consistency"
    - "Technical terms preserved in English (Bearer, token, API key, etc.)"
  artifacts:
    - path: "docs-site/content/ja/docs/configuration.md"
      provides: "Authentication Architecture section in Japanese"
      contains: "allow_subscription"
    - path: "docs-site/content/zh-cn/docs/configuration.md"
      provides: "Authentication Architecture section in Simplified Chinese"
      contains: "allow_subscription"
    - path: "docs-site/content/ko/docs/configuration.md"
      provides: "Authentication Architecture section in Korean"
      contains: "allow_subscription"
  key_links:
    - from: "docs-site/content/ja/docs/configuration.md"
      to: "docs-site/content/en/docs/configuration.md"
      via: "translation of same content"
      pattern: "allow_subscription"
---

<objective>
Translate Authentication Architecture section to Japanese, Chinese, and Korean

Purpose: Ensure Japanese, Chinese, and Korean speaking users understand the two-tier authentication model and subscription token limitations.

Output:
1. Japanese configuration.md with translated "Authentication Architecture" section
2. Chinese (Simplified) configuration.md with translated section
3. Korean configuration.md with translated section
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.2-subscription-token-relay/02.2-RESEARCH.md

# Source content (from Plan 01)
@docs-site/content/en/docs/configuration.md

# Prior translation pattern
@.planning/phases/02.1-site-docs-multi-key-pooling/02.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Authentication Architecture section to Japanese configuration.md</name>
  <files>docs-site/content/ja/docs/configuration.md</files>
  <action>
Translate the Authentication Architecture section from English to Japanese. Add it BEFORE the existing authentication section.

Translation guidelines:
- Keep all YAML code blocks in English (only translate comments)
- Keep technical terms in English: Bearer, token, API key, x-api-key, Authorization
- Translate explanatory text, section headings, and descriptions
- Use polite Japanese (desu/masu form)

Section heading in Japanese (suggested): "### 認証アーキテクチャ"

Key concepts to translate:
- "Two-tier authentication model" -> "二層認証モデル"
- "Client-to-Proxy" -> "クライアントからプロキシ"
- "Proxy-to-Backend" -> "プロキシからバックエンド"
- "Subscription tokens are proxy-only" -> "サブスクリプショントークンはプロキシ認証のみです"
- "You MUST configure provider API keys" -> "プロバイダーAPIキーを設定する必要があります"
- "proxy authentication only" -> "プロキシ認証のみ"

Also update the existing subscription passthrough subsection to include the warning.

Verify Hugo builds:
```bash
cd docs-site && hugo --minify
```
  </action>
  <verify>
1. `cd /home/omarluq/sandbox/go/cc-relay/docs-site && hugo --minify` completes without errors
2. Japanese authentication architecture section exists in the file
3. YAML code blocks are preserved in English
  </verify>
  <done>
- Japanese configuration.md has translated "Authentication Architecture" section
- Warning about API key requirement is translated
- Hugo builds successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Authentication Architecture section to Chinese configuration.md</name>
  <files>docs-site/content/zh-cn/docs/configuration.md</files>
  <action>
Translate the Authentication Architecture section from English to Simplified Chinese. Add it BEFORE the existing authentication section.

Translation guidelines:
- Keep all YAML code blocks in English (only translate comments)
- Keep technical terms in English: Bearer, token, API key, x-api-key, Authorization
- Translate explanatory text, section headings, and descriptions
- Use Simplified Chinese characters

Section heading in Chinese (suggested): "### 认证架构"

Key concepts to translate:
- "Two-tier authentication model" -> "双层认证模型"
- "Client-to-Proxy" -> "客户端到代理"
- "Proxy-to-Backend" -> "代理到后端"
- "Subscription tokens are proxy-only" -> "订阅令牌仅用于代理认证"
- "You MUST configure provider API keys" -> "您必须配置提供商 API 密钥"
- "proxy authentication only" -> "仅代理认证"

Also update the existing subscription passthrough subsection to include the warning.

Verify Hugo builds:
```bash
cd docs-site && hugo --minify
```
  </action>
  <verify>
1. `cd /home/omarluq/sandbox/go/cc-relay/docs-site && hugo --minify` completes without errors
2. Chinese authentication architecture section exists in the file
3. YAML code blocks are preserved in English
  </verify>
  <done>
- Chinese configuration.md has translated "Authentication Architecture" section
- Warning about API key requirement is translated
- Hugo builds successfully
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Authentication Architecture section to Korean configuration.md</name>
  <files>docs-site/content/ko/docs/configuration.md</files>
  <action>
Translate the Authentication Architecture section from English to Korean. Add it BEFORE the existing authentication section.

Translation guidelines:
- Keep all YAML code blocks in English (only translate comments)
- Keep technical terms in English: Bearer, token, API key, x-api-key, Authorization
- Translate explanatory text, section headings, and descriptions
- Use formal Korean (습니다 form)

Section heading in Korean (suggested): "### 인증 아키텍처"

Key concepts to translate:
- "Two-tier authentication model" -> "이중 인증 모델"
- "Client-to-Proxy" -> "클라이언트에서 프록시로"
- "Proxy-to-Backend" -> "프록시에서 백엔드로"
- "Subscription tokens are proxy-only" -> "구독 토큰은 프록시 인증에만 사용됩니다"
- "You MUST configure provider API keys" -> "제공업체 API 키를 구성해야 합니다"
- "proxy authentication only" -> "프록시 인증만"

Also update the existing subscription passthrough subsection to include the warning.

Verify Hugo builds:
```bash
cd docs-site && hugo --minify
```
  </action>
  <verify>
1. `cd /home/omarluq/sandbox/go/cc-relay/docs-site && hugo --minify` completes without errors
2. Korean authentication architecture section exists in the file
3. YAML code blocks are preserved in English
  </verify>
  <done>
- Korean configuration.md has translated "Authentication Architecture" section
- Warning about API key requirement is translated
- Hugo builds successfully
  </done>
</task>

</tasks>

<verification>
1. Hugo site builds without errors: `cd docs-site && hugo --minify`
2. All three languages have new auth section content
3. YAML blocks preserved in English in all files
4. No build warnings or missing translations
</verification>

<success_criteria>
1. Japanese configuration.md has translated "Authentication Architecture" section
2. Chinese configuration.md has translated "Authentication Architecture" section
3. Korean configuration.md has translated "Authentication Architecture" section
4. All YAML code blocks remain in English
5. Hugo builds successfully for all languages
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-subscription-token-relay/02.2-03-SUMMARY.md`
</output>
